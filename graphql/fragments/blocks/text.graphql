fragment textBlock on TextRecord {
  id
  anchorId
  disableMarginTop
  disableMarginBottom
  content {
    value
    links {
      ... on PageRecord {
        __typename
        id
        slug
        parent {
          slug
        }
      }
    }
  }
}
